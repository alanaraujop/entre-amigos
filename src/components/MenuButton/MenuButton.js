import React from 'react';
import { Button, ButtonLine } from './styles';

const MenuButton = () => {
  
  return (
    <Button>
      <ButtonLine />
      <ButtonLine />
      <ButtonLine />
    </Button>
  );
};

export default MenuButton;
